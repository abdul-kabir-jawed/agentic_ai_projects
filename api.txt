during the registration we have can ask the user for there gemini api key and openai api key its optional without that user cant access the ai chat page or chatbox they cant use it and the user can update the api or add the api in the profile page too we store that in the table with a column of api having a dictionary having gemini and openai api keys in a way that its protected encrypted for user safety so we dont have too add the api in env by ourself each user can give there own api key so the api of gemini and openai will not be given from the env but from the user